Commands
========

Here is a list of all the program's commands for easy navigation. Each links simply to its help output as obtainable via ``simplelogin <command> --help``.

.. toctree::

   account/account
   alias/alias
   config/config
   database/database
   mailbox/mailbox
